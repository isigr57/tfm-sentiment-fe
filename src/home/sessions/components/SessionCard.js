
import { Box } from '@mui/material';


const SessionCard = ({session, handleEdit, handleDelete}) => {
    return (
        <Box sx={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
            Test
        </Box>
    );
}
export default SessionCard;
